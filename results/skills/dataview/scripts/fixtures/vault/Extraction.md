# Extraction edge cases

> [!example]
> ~~~~dataview
> TABLE status
> FROM "Projects"
> WHERE status = "open"
> ~~~~~

The configured inline prefix below is inside a generic code block and must be ignored:

```text
`dv= date(today)`
```
